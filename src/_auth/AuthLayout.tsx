import React from "react";
// Below layout is going to wrap both sign in and sign up forms.
// It will both the lago and the image gallery that appears when the website is opened,
// and the forms of sign in and sign up will change according to the URL.
const AuthLayout = () => {
  return <div>AuthLayout</div>;
};

export default AuthLayout;
